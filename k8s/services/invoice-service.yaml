
apiVersion: v1
kind: Service
metadata:
  name: invoice-service
  namespace: hk-fintech
  labels:
    app: invoice-service
spec:
  type: ClusterIP
  ports:
    - port: 9094
      targetPort: 9094
  selector:
    app: invoice-service

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: invoice-service
  namespace: hk-fintech
  labels:
    app: invoice-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: invoice-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: invoice-service
    spec:
      initContainers:
        - name: wait-for-db
          image: busybox:1.28
          command: ['sh', '-c', 'until nc -z postgres-invoice 5432; do echo waiting for invoice db; sleep 2; done;']
      containers:
        - name: invoice-service
          image: hk-fintech-hk-fintech-invoice-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9094
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "k8s"
            # ---- Veritabanı ----
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: hk-fintech-secret
                  key: POSTGRES_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: hk-fintech-secret
                  key: POSTGRES_PASSWORD
          resources:
            requests:
              cpu: 200m
              memory: 384Mi
            limits:
              cpu: 1000m
              memory: 768Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9094
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9094
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 3
