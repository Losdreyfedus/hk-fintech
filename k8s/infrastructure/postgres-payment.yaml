# =====================================================
# POSTGRESQL — Payment DB (StatefulSet + Service + PVC)
# =====================================================
apiVersion: v1
kind: Service
metadata:
  name: postgres-payment
  namespace: hk-fintech
  labels:
    app: postgres-payment
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres-payment

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-payment
  namespace: hk-fintech
spec:
  serviceName: postgres-payment
  replicas: 1
  selector:
    matchLabels:
      app: postgres-payment
  template:
    metadata:
      labels:
        app: postgres-payment
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: hk-fintech-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: hk-fintech-secret
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              value: "payment_db"
          volumeMounts:
            - name: postgres-payment-storage
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
  volumeClaimTemplates:
    - metadata:
        name: postgres-payment-storage
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
